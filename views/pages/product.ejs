<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Productos</title>
    <link rel="stylesheet" type="text/css" href="./../../public/css/style.css" />
</head>
<body>
    <a href='/' class="btnLink">Cargar Producto Nuevo</a>
    <h1 class='titulo'><%= titulo %></h1>
    <div class='contenedorProductos'>
        <% if (productsList) { %>
            <div class="producto" id="producto-<%= productsList.id %>">
                <img src="<%= productsList.thumbnail %>" alt='imagen de producto' class="productoImagen">
                <div class="productoTextos">
                    <p class="productoTitulo"><%= productsList.title %></p>
                    <p class="productoPrecio">$ <%= productsList.price %></p>
                </div>
            </div>
        <% } else { %>
            <div class="contenedorNotFound">
                <p class="textoNotFound">No se encontraron productos, ingrese otro id</p>
            </div>
        <% } %>
    </div>
</body>
</html>